name: waffleup_brand_kit
description: WaffleUp brand design tokens for Flutter applications.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
